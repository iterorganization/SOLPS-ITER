FC	 = f90

ifdef SOLPS_DEBUG
FFLAGS	 = -g 
else
FFLAGS	 = -O 
endif

INCLUDE = -I${SRCDIR} -I${EIRINC} -I${B2INC}
COMPILE  = ${FC} -c ${FFLAGS}
CPP	 = /usr/ccs/lib/cpp
MOD 	 = mod

LDLIBS 	 = -L${SOLPS_LIB} -lmscl
LDEXTRA  = 
