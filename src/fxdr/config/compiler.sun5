FC	 = f77

ifdef SOLPS_DEBUG
FFLAGS	 = -g
else
FFLAGS	 = -O
endif

INCLUDE  = -Isrc
COMPILE  = ${FC} -c ${FFLAGS}
CPP	 = /usr/ccs/lib/cpp

LDLIBS	 = -L${SOLPS_LIB} -lsonnet3.1 -lmscl -L/usr/local/lib -lC -lw -lnsl
LDEXTRA  = 
