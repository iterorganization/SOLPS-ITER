# Load modules
module load solps-libs/pgi

if (-e /usr/local/texlive/bin/x86_64-linux) then 
setenv PATH /usr/local/texlive/bin/x86_64-linux:${PATH}
endif

# Environment variables
setenv DEVICE d3d
echo $DEVICE

# Debugger
setenv DBX gdb
