include config/config.${HOST_NAME}.${COMPILER}
ifeq ($(shell [ -e config/config.common.${COMPILER} ] && echo yes || echo no ),yes)
  include config/config.common.${COMPILER}
  MAKES += config/config.common.${COMPILER}
endif

${OBJDIR}/%.o : %.F
	@- /bin/rm -f ${OBJDIR}/$*.f ${OBJDIR}/$*.o ${OBJDIR}/$*.${MOD}
ifeq ($(strip $(CPP)),)
	case $< in \
		${S45DIR}/src/solps4_*/* ) ${DBLFC} ${DBLOPTION} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${EQUIV4} ${SOLPSINCLUDE} ${OBJDEST} -c $< ;; \
		${S45DIR}/local_4/* ) ${DBLFC} ${DBLOPTION} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${EQUIV4} ${SOLPSINCLUDE} ${OBJDEST} -c $< ;; \
                * ) ${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${EQUIV5} ${SOLPSINCLUDE} ${OBJDEST} -c $< ;; \
        esac
else
ifeq ($(strip $(SED)),)
	case $< in \
		${S45DIR}/src/solps4_*/* ) ${CPP} ${CPP4OPTS} ${DEFINES} ${SOLPSINCLUDE} $< ${OBJDIR}/$*.f ;; \
		${S45DIR}/local_4/* ) ${CPP} ${CPP4OPTS} ${DEFINES} ${SOLPSINCLUDE} $< ${OBJDIR}/$*.f ;; \
                * ) ${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< ${OBJDIR}/$*.f ;; \
        esac
else
	case $< in \
		${S45DIR}/src/solps4_*/* ) ${CPP} ${CPP4OPTS} ${DEFINES} ${SOLPSINCLUDE} $< | ${SED} > ${OBJDIR}/$*.f ;; \
		${S45DIR}/local_4/* ) ${CPP} ${CPP4OPTS} ${DEFINES} ${SOLPSINCLUDE} $< | ${SED} > ${OBJDIR}/$*.f ;; \
                * ) ${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< | ${SED} > ${OBJDIR}/$*.f ;; \
        esac
endif
	case $< in \
		${S45DIR}/src/solps4_*/* ) ${DBLFC} ${DBLOPTION} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${SOLPSINCLUDE} ${INCMOD}${OBJDIR} ${OBJDEST} -o ${OBJDIR}/$*.o -c ${OBJDIR}/$*.f ;; \
		${S45DIR}/local_4/* ) ${DBLFC} ${DBLOPTION} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${SOLPSINCLUDE} ${INCMOD}${OBJDIR} ${OBJDEST} -o ${OBJDIR}/$*.o -c ${OBJDIR}/$*.f ;; \
                * ) ${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${SOLPSINCLUDE} ${INCMOD}${OBJDIR} ${OBJDEST} -o ${OBJDIR}/$*.o -c ${OBJDIR}/$*.f ;; \
        esac
endif
	@- if [ -f $*.o ] ; then /bin/mv $*.o ${OBJDIR}/ ; fi
	@- if [ -f $*.${MOD} ] ; then /bin/mv $*.${MOD} ${OBJDIR}/ ; fi
ifneq ($(COMPILER),nag_f90)
	@if [ -f ${OBJDIR}/$*.${MOD} ] ; then touch ${OBJDIR}/$*.${MOD} ; fi
endif

${OBJDIR}/%.o : %.F90
	@- /bin/rm -f ${OBJDIR}/$*.f90 ${OBJDIR}/$*.o ${OBJDIR}/$*.${MOD}
ifeq ($(strip $(CPP)),)
	${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${EQUIV5} ${SOLPSINCLUDE} ${OBJDEST} -c $<
else
ifeq ($(strip $(SED)),)
	${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< ${OBJDIR}/$*.f90
else
	${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< | ${SED} > ${OBJDIR}/$*.f90
endif
	${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${SOLPSINCLUDE} ${INCMOD}${OBJDIR} ${OBJDEST} -o ${OBJDIR}/$*.o -c ${OBJDIR}/$*.f90
endif
	@- if [ -f $*.o ] ; then /bin/mv $*.o ${OBJDIR}/ ; fi
	@- if [ -f $*.${MOD} ] ; then /bin/mv $*.${MOD} ${OBJDIR}/ ; fi
ifneq ($(COMPILER),nag_f90)
	@if [ -f ${OBJDIR}/$*.${MOD} ] ; then touch ${OBJDIR}/$*.${MOD} ; fi
endif

${OBJDIR}/%.${MOD} : %.F        # src/B2.5_modules/
	@- /bin/rm -f ${OBJDIR}/$*.f ${OBJDIR}/$*.o ${OBJDIR}/$*.${MOD}
ifeq ($(strip $(CPP)),)
	${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${EQUIV5} ${SOLPSINCLUDE} ${OBJDEST} -c $<
else
ifeq ($(strip $(SED)),)
	-${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< ${OBJDIR}/$*.f
else
	-${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< | ${SED} > ${OBJDIR}/$*.f
endif
	${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${SOLPSINCLUDE} ${INCMOD}${OBJDIR} ${OBJDEST} -o ${OBJDIR}/$*.o -c ${OBJDIR}/$*.f
endif
	@- if [ -f $*.o ] ; then /bin/mv $*.o ${OBJDIR}/ ; fi
ifeq ($(strip $(LINK_MOD)),)
	@- if [ -f $*.${MOD} ] ; then /bin/mv $*.${MOD} ${OBJDIR}/ ; fi
else
	@- [ -f $*.${MOD} ] && [ ! -L $*.${MOD} ] && /bin/mv $*.${MOD} ${OBJDIR}/ && /bin/ln -s ${OBJDIR}/$*.${MOD} . || echo > /dev/null
endif
ifneq ($(COMPILER),nag_f90)
	@if [ -f ${OBJDIR}/$*.${MOD} ] ; then touch ${OBJDIR}/$*.${MOD} ; fi
endif

${OBJDIR}/%.${MOD} : %.F90        # src/solps5/b2mod_*.F90
	@- /bin/rm -f ${OBJDIR}/$*.f90 ${OBJDIR}/$*.o ${OBJDIR}/$*.${MOD}
ifeq ($(strip $(CPP)),)
	${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${EQUIV5} ${SOLPSINCLUDE} ${OBJDEST} -c $<
else
ifeq ($(strip $(SED)),)
	-${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< ${OBJDIR}/$*.f90
else
	-${CPP} ${CPP5OPTS} ${DEFINES} ${SOLPSINCLUDE} $< | ${SED} > ${OBJDIR}/$*.f90
endif
	${FC} ${FCOPTS} ${FFLAGSEXTRA} ${DEFINES} ${SOLPSINCLUDE} ${INCMOD}${OBJDIR} ${OBJDEST} -o ${OBJDIR}/$*.o -c ${OBJDIR}/$*.f90
endif
	@- if [ -f $*.o ] ; then /bin/mv $*.o ${OBJDIR}/ ; fi
ifeq ($(strip $(LINK_MOD)),)
	@- if [ -f $*.${MOD} ] ; then /bin/mv $*.${MOD} ${OBJDIR}/ ; fi
else
	@- [ -f $*.${MOD} ] && [ ! -L $*.${MOD} ] && /bin/mv $*.${MOD} ${OBJDIR}/ && /bin/ln -s ${OBJDIR}/$*.${MOD} . || echo > /dev/null
endif
ifneq ($(COMPILER),nag_f90)
	@if [ -f ${OBJDIR}/$*.${MOD} ] ; then touch ${OBJDIR}/$*.${MOD} ; fi
endif

#!!!Local Variables:
#!!! mode: Makefile
#!!! End:

