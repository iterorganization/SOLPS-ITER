CPP = /lib/cpp -traditional

ifdef SOLPS_DEBUG
FCOPTS = -g -fbounds-check -fbacktrace -fdefault-real-8 -ffpe-trap=denormal
endif

SOLPSINCLUDE += $(shell pkg-config libtirpc --cflags)
LD_SONNET += $(shell pkg-config libtirpc --libs)

#!!!Local Variables:
#!!! mode: Makefile
#!!! End:
