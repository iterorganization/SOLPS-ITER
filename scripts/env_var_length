#! /usr/bin/env python

# This script (provided by David Coster) prints the length of strings
# corresponding to each environment variable

# Example of usage: `env_var_length | sort -n | tail` will sort environment
# variables by length and print 10 longest ones.

import os
for i in os.environ: print(len(os.environ[i]), i)

