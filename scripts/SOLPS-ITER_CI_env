# Set up environment for SOLPS-ITER compilation and test runs

set -v

. /usr/share/Modules/init/sh

export MAKE=/usr/bin/gmake
echo "export HOST_NAME=ITER" > ./SETUP/setup.ksh.HOST_NAME.local
. setup.ksh ifort64
make local
