#! /bin/ksh

#  VERSION : 02.09.99 14:23

Name=`basename $0`
Usage="$Name: Pietro-to-DG format conversion
Usage: $Name Pietro-file DG-file
       where DG-file is the output filename,
       and Pietro-file is the input filename.
"

[ $# -lt 2 ] && { echo "$Usage"; exit; }
[ -r $1 ] || { echo "File $1 is absent\n"; echo "$Usage"; exit; }
rm -f fort.2 > /dev/null
ln -sf $1 fort.1
$SOLPSTOP/src/DivGeo/dg/equtrn/$OBJECTCODE/pb2dg && { mv fort.2 $2; rm -f fort.1; }
