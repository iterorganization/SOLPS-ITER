#! /usr/bin/ksh

# version : 18.12.2001 00:03

Name=${0##*/}

Usage="$Name removes the data files from the traces_all directory.
  Usage: $Name -
"

[ -z "$1" ] && {
  print -- "$Usage"
  exit 4
}

[ -d traces_all ] && cd traces_all && rm *.trc* traces* *.cmd *.log *.set *_outlist datefirst *.dat
